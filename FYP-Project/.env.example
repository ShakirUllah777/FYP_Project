SECRET_KEY=django-insecure-anyrandomstringhere123
DEBUG=True

# --- AI features (assistant app: AI Suggester, chatbot, resume extractor, scope estimator) ---
# Leave blank to run entirely on the built-in rule-based fallback (no external calls, no cost).
# Add a real key to make the AI Suggester / Chatbot / Resume Extractor genuinely LLM-powered.
ANTHROPIC_API_KEY=

# --- Email (verification links + 2FA login codes) ---
# Defaults to the console backend, so verification/OTP emails print to the
# runserver terminal instead of requiring a real SMTP account.
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# DEFAULT_FROM_EMAIL=no-reply@collabspace.local
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=True
